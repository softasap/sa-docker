- name: Docker Composer | Installing pypi package
  pip: name=docker-compose  version="{{ docker_compose_version if docker_compose_version else omit }}" virtualenv="{{ docker_compose_venv_directory if docker_compose_venv_directory else omit }}" state=present
  become: yes
  tags:
    - docker-compose
